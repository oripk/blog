<!-- views/home/welcome.ejs -->

<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>

    <div class="container mb-3">

      <div class="jumbotron">
        <h1>My Website</h1>
        <P>제 웹사이트를 방문해 주셔서 감사합니다!<br>
        이 웹사이트는 기분에 따라서 변동 될 수 있습니다.</p> 
      </div>

      <h3 class="mb-4 pb-4 border-bottom">목록</h3>
      <div class="row row-cols-1 row-cols-md-4">
      <% if(posts == null || posts.length == 0){ %>
        <div class="text-primary"> There is no data to show :( </div>
      <% } %>
      <% posts.forEach((post, index) => { %>
        <% if(index == 3) return false;%>
        <div class="col mb-4">
          <div class="card">
            <div class="card-body">
              <h5 class="card-title"><%= post.title %></h5>
              <p class="card-text"><%= post.body %></p>
              <a class="btn btn-primary" href="/posts/<%= post._id %>">바로가기</a> 
            </div>
          </div>
        </div>
      <% }) %>    
      </div>
    </div>
  </body>
</html>